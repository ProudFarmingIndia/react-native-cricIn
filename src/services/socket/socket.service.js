let socket = null;

export const connectSocket =
  () => {

    console.log(
      'Socket Connected',
    );

  };

export const disconnectSocket =
  () => {

    console.log(
      'Socket Disconnected',
    );

  };

export default socket;